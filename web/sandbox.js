

var http = require("http")

// new
var handler = function(req,resp){
    // new
}

var server = htpttp.create(handler)
  server.lister


  handler = fcuntion(res, res){
    if( req.url==='/'){

    }else if( req.url==='/save' ){
        if( ==="post")
          var saved = "";
          req.on("data", function(chunk)){
          saved += chunk
        }
      }

    }


  }